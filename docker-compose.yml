services:
  web:
    container_name: bable-web
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "4173:4173"
    environment:
      PORT: "4173"
      NODE_ENV: production
    restart: unless-stopped
